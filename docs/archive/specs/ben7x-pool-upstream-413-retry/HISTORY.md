# 演进记录

## 2026-04-27

- 明确上游 `413` 的产品意图：原账号补试用于过滤偶发上游误报，持续失败后应切换账号规避异常上游。
- 锁定实现默认：规则应用于所有 pool 账号类型，切号范围沿用现有 pool failover 上限和 timeout 预算，不新增配置项。
- 新增 `upstream_http_413` failure kind，避免与本地请求体过大错误混淆。
