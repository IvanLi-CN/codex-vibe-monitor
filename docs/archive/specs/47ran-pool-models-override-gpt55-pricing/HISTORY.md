# 47ran History

- 2026-04-25: 创建 spec，记录“仅恢复当前 pool `/v1/models` 路径级覆盖，不恢复非 pool `/v1/*`”的范围。
- 2026-04-25: 落地 backend settings proxy 契约、pool `/v1/models` hijack/merge/fallback、GPT-5.5 默认 preset/pricing、Settings UI 与 Storybook mock。
