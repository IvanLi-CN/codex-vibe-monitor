# Release `latest` 与当前 CI Main stable - History

## Lifecycle

- Immutable release snapshots continue to preserve merged-PR release intent independently of whether a separate public tag is issued.
- Automatic historical queue continuation was replaced by current-successful-CI targeting. See `docs/adr/0008-release-current-ci-head-coalescing.md`.
