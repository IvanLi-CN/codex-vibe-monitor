# 规格（Spec）总览

本目录用于管理工作项的规格与追踪：记录范围、验收标准、任务清单与状态，作为交付依据；实现与验证应以对应 `SPEC.md` 为准。

> Legacy compatibility: historical plans remain in `docs/plan/**/PLAN.md`. New entries are created in `docs/specs/**/SPEC.md`.

## Index

| ID    | Title                                                                         | Status             | Spec                                                     | Last       | Notes                                                                                                                                              |
| ----- | ----------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3np57 | 绑定代理节点改为“本组真实流量统计”                                            | 已完成（5/5，PR #343） | `3np57-group-bound-proxy-real-traffic-stats/SPEC.md`     | 2026-04-13 | fast-track / group settings dialog `24H` node stats switch from global forward-proxy hourly attempts to group-scoped real pool request attempts / snapshot `group_name_snapshot + proxy_binding_key_snapshot` from fix-forward only / `groupName` query keeps old endpoint behavior when omitted |
| r99mz | Dashboard：把“今日”并入“活动总览”，并为今日新增分钟级柱状 / 累计面积图 | 已实现，PR #321 收敛中 | `r99mz-dashboard-today-activity-overview/SPEC.md` | 2026-04-10 | fast-track / follow-up of `#7s4kw` + `#s8d2w` + `#3xaa3` / Dashboard top-level today card removed in favor of overview-owned `今日 / 24 小时 / 7 日 / 历史` / today adds minute-level success-vs-failure bars plus cumulative cost/token areas / success+failure bars now share the same minute slot around the zero axis / today-count now renders neutral positive bars only from explicit `inFlightCount`, so legacy neutral residuals in `totalCount` are no longer mispainted as in-flight / authoritative + live + archive now consistently keep nonterminal `running/pending` out of failure totals / legacy blank/null status rows remain neutral unless explicit failure metadata exists, and prompt-cache keyed charts/tables now render those rows with neutral tone instead of error red while authoritative/live rows with explicit failure metadata immediately stay in failure tone even before `failureClass` or terminal status fully settle / authoritative prompt-cache `last24hRequests` now also treats `errorMessage` + `downstreamErrorMessage` + `failureKind` as explicit failure metadata, so refreshes no longer downgrade running/pending failed rows back to `in_flight` or neutral / downstream-only legacy failure rows now stay out of `success/neutral` fast paths in summary + timeseries repair, exact/live aggregate readers now project resolved failure metadata, and live today buckets also flip to failure immediately when only `downstreamErrorMessage` is present / legacy `completed` success rows now stay success-like across `query_combined_totals`, exact/live summary helpers, hourly rollups, invocation summary, and today TTFB samples instead of being undercounted as failure or dropped from success-only latency metrics / in-flight seed paging now reuses a stable `snapshotId` across `running` + `pending` / anonymous in-flight placeholders only match same-bucket records created before the authoritative load, so fresh same-bucket arrivals no longer suppress minute totals / placeholder reclaim now keys off authoritative `snapshotId`, while silent refresh merges tracked live deltas back into the fresh response so duplicate settled SSE and clock drift no longer double-count / anonymous-placeholder settle now corrects provisional token/cost straight to the final value without waiting for a full reload / hourly-backed summary+timeseries+failure readers now refresh rollups, freeze a shared `snapshotId`, and exact-replay the `rollup_live_cursor -> snapshotId` full-hour tail so archived rollups stay aligned with live rows / legacy `http_200` success-like rows are counted back into invocation summary success totals / current-day-local seed fetches are narrowed to the active day bucket instead of paging the entire long-range window / mixed preserve repair now clears each bucket/source once for every replayed existing archive, including non-materialized batches, so stale failure counts are corrected without double-counting / boundary archive/live crossover buckets now rehydrate any previously rolled live rows up to the shared cursor after replay, so all-time repair no longer drops mixed-hour totals / failure-only replay-marker backfills now preserve already-correct `invocation_rollup_hourly` totals while repairing the missing failure-side replay state / historical hourly-backed timeseries + summary + failure views now force the same repair/backfill path before reading archived rollups, so upgrade no longer leaves long-range charts stale / remount-cache silent refresh preserves recent settled delta memory, absorbs duplicate settled SSE after rehydrate, and no longer restores buckets that already slid out of the refreshed range / latest `main` freshness sync merged cleanly and prompt-cache paging query regressions stay green / post-sync validation reran on the current convergence head with `web` full test/build/storybook plus targeted cargo regressions green / Dashboard KPI overflow now compacts to `1.31B` when needed / prompt-cache working set stays bounded / Storybook evidence archived in spec |
| nepye | node shunt 同步路径共享绑定节点恢复                                          | 已实现，待 PR / CI / review-proof 收敛 | `nepye-sync-node-shunt-nonexclusive-recovery/SPEC.md`    | 2026-04-07 | fast-track / follow-up of `#6b9ra` + `#js2gr` / OAuth sync-only resolver now reuses same-account pinned node first, then shared bound-group probe / exclusive live routing semantics unchanged / local cargo fmt+check+full cargo test green |
| 7s4kw | Dashboard：把“历史”并入“活动总览”，并将“今日统计信息”改为单行 KPI          | 已实现，待 PR / CI / review-proof 收敛 | `7s4kw-dashboard-usage-activity-overview/SPEC.md`        | 2026-04-07 | fast-track / follow-up of `#dzbnx` + `#rzxey` + `#s8d2w` + `#h5k2r` / history tab replaces usage activity and expands the calendar from `90d` to `6mo` / embedded history view drops duplicate title+timezone and tightens month-label spacing / visual evidence archived in spec / local vitest+build+storybook green |
| h9r2m | 历史在线统计改为主库 hourly rollup 唯一真相源                                 | 已实现             | `h9r2m-permanent-online-hourly-rollups/SPEC.md`          | 2026-03-25 | fast-track / permanent hourly rollups / archive backup-only / stats live-tail merge / pool attempts live-only / local + shared-testbox validation green |
| e6082 | Prompt Cache 图表时间轴 24 小时封顶热修                                       | 已完成（4/4）      | `e6082-prompt-cache-chart-window-24h-cap/SPEC.md`        | 2026-03-20 | fast-track / hotfix / PR #190 / prompt-cache chart window hard-capped to 24h / local validation green / spec-sync                                 |
| m4c2q | Prompt Cache Key 对话筛选增强与动态时间轴                                     | 已完成（4/4）      | `m4c2q-prompt-cache-conversation-filter-window/SPEC.md`  | 2026-03-19 | fast-track / prompt-cache conversations / count+activity filter / dynamic shared chart range / storybook visual evidence synced                  |
| cgz7s | OAuth 凭据 JSON 批量导入与验活                                                | 已完成（4/4）      | `cgz7s-oauth-token-json-import/SPEC.md`                  | 2026-03-22 | fast-track / upstream account import / empty expired fallback via access_token.exp then id_token.exp / validation-jobs SSE / import reuses validation cache |
| quhzx | 建立全局 UI 规范文档体系                                                      | 已完成（5/5）      | `quhzx-ui-guidelines-system/SPEC.md`                     | 2026-03-18 | fast-track / docs-only / docs/ui / PR #173 / checks green / review-loop clear                                                                      |
| 62uhg | 上游账号列表紧凑化改版                                                        | 已完成（5/5）      | `62uhg-upstream-account-roster-compact/SPEC.md`          | 2026-03-27 | fast-track / compact roster / missing secondary window placeholders unified to weak ASCII `-` / real `0%` snapshots preserved / storybook evidence synced / local targeted vitest+build+storybook green |
| m7a9k | OAuth 手动邮箱附着与增强能力判定                                              | 进行中             | `m7a9k-oauth-manual-mailbox-attach/SPEC.md`              | 2026-03-17 | fast-track / single oauth + reauth manual mailbox attach / mailboxAddress contract / review pending                                                |
| e5w9m | 批量 OAuth 邮箱气泡编辑与邮箱动作解耦                                         | 进行中             | `e5w9m-batch-oauth-mailbox-popover-edit/SPEC.md`         | 2026-03-17 | fast-track / batch mailbox popover edit / single-button busy split / review pending                                                                |
| ynr8z | 号池流式上游误用整请求超时                                                    | 进行中             | `ynr8z-pool-stream-total-timeout/SPEC.md`                | 2026-03-17 | fast-track / pool streaming / shared client timeout / no replay semantic change                                                                    |
| q1m9k | Release PR 评论权限补齐                                                       | 进行中             | `q1m9k-release-pr-comment-permission/SPEC.md`            | 2026-03-17 | fast-track / release workflow / pull-requests write / comment permission fix                                                                       |
| f7nqn | InvocationTable 代理节点展示热修                                              | 已完成             | `f7nqn-invocation-proxy-display-restore-hotfix/SPEC.md`  | 2026-03-17 | fast-track / hotfix / proxy semantics restored / compact badge matrix / screenshot evidence synced                                                 |
| sq8gw | Release 工作流 PR 版本评论                                                    | 进行中             | `sq8gw-release-pr-version-comment/SPEC.md`               | 2026-03-17 | fast-track / release publish / PR version comment upsert / review convergence                                                                      |
| 3n287 | OAuth 临时邮箱自动化与验证码/邀请态集成                                       | 进行中             | `3n287-oauth-temp-mail-automation/SPEC.md`               | 2026-03-17 | fast-track / MoeMail mailbox sessions baseline / manual mailbox attach moved to m7a9k                                                              |
| f6f6e | GH Actions 防取消发布链路全面对齐                                             | 部分完成（3/4）    | `f6f6e-gh-actions-release-anti-cancel/SPEC.md`           | 2026-03-15 | strict anti-cancel / global serial CI Main+Release / current PR labels / contract refresh                                                          |
| 7n2ex | InvocationTable 账号归因、时延压缩展示与当前页账号抽屉                        | 已完成             | `7n2ex-invocation-account-latency-drawer/SPEC.md`        | 2026-03-17 | fast-track / invocation account attribution + latency compression + inline account drawer / latency units evidence synced                          |
| ----- | ----------------------------------------------------------------------------- | ---------------    | -------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------                                                     |
| pd77h | OAuth 数据面内联合并                                                          | 待实现             | `pd77h-oauth-inline-adapter/SPEC.md`                     | 2026-03-16 | fast-track / single-process oauth adapter / remove sidecar / no compatibility shim                                                                 |
