# History

The account model catalog was introduced to make upstream capability discovery explicit and account-scoped. It complements the existing project preset model list without changing routing policy semantics or the model-mapping contract.
