services:
  backend-test:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-test
    working_dir: /workspace
    entrypoint: []
    command: ["sleep", "infinity"]
    user: "65534:65534"
